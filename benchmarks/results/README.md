# Raw results

Store immutable JSON output here using `v<workload>-<arch>-<os>-<date>-run<N>.json` and include a
matching Markdown hardware record. No baseline is checked in yet because results generated inside
an unknown or contended development environment would be misleading.
