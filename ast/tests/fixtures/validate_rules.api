syntax = "v1"

struct PayReq {
    #[json("order_id")]
    #[validate(required, length(1, 32), one_of("cny", "usd"))]
    orderId: String,
    #[validate(range(1, 1000000))]
    amount: i64,
}

service pay {
    post /pay (PayReq)
}
